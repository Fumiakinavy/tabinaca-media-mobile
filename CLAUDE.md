# コーディングエージェント向け指示

- 参照元は `context/tabinaka-media-staging-develop` のWebサイト。`context/` は編集禁止（読み取り専用）。
- 必須フロー: Webを理解 → `plan/` にMarkdownの計画書作成 → iOS実装（`tabinaca-media-ios/`）。
- 計画書ができるまでコード変更は禁止。途中で要件が変わったら、まず計画書を更新する。
- iOS側はWebのUI/UXにできる限り忠実に合わせる。必要ならアセットをコピーするが元は編集しない。
- 日本語で簡潔に報告し、不明点は確認する。
